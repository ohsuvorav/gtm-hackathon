[project]
name = "info-to-content-plugin"
version = "0.1.0"
description = "Deterministic state and validation helpers for the InfoToContent Codex plugin"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.8,<3",
]

[tool.uv]
package = false
